name: Pipeline

on:
  push:
    branches:
      - main

jobs:
  exemplo:
    runs-on: ubuntu-latest

    steps:
      - name: Mostrar variável
        run: echo "URL: ${{ vars.URL_AMBIENTE }}"

      - name: Mostrar secret
        run: echo "Token: ${{ secrets.API_TOKEN_PROD }}"
